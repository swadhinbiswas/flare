---
import '@/styles/globals.css';
import { Toaster } from '@/components/ui/sonner';

interface Props {
  title?: string;
  description?: string;
  bodyClass?: string;
  /** Center content in a full-viewport flex container (login, errors). */
  centered?: boolean;
}

const {
  title = 'RFLARE',
  description = 'Self-hosted mailbox on Resend, Cloudflare Workers and Turso.',
  bodyClass = '',
  centered = false,
} = Astro.props;
---

<!doctype html>
<html lang="en" class="dark" data-theme="proton">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#0f0f11" />
    <meta name="color-scheme" content="dark light" />
    <title>{title}</title>
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <link rel="canonical" href={Astro.url.href} />
    <script is:inline>
      // Apply the stored palette and mode before first paint so there is no
      // flash. Keys must match src/lib/theme.ts.
      try {
        var theme = localStorage.getItem('rflare-theme-name') || 'proton';
        var mode = localStorage.getItem('rflare-theme') || 'dark';
        document.documentElement.dataset.theme = theme;
        document.documentElement.classList.toggle('dark', mode !== 'light');
      } catch (error) {
        /* storage unavailable */
      }
    </script>
  </head>
  <body class:list={['min-h-dvh bg-background text-foreground antialiased', centered && 'grid place-items-center', bodyClass]}>
    <slot />
    <Toaster client:load />
  </body>
</html>
