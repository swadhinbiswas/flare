# Do not install packages published in the last 24 hours.
# Keeps local installs and CI lockfile verification in agreement.
minimum-release-age=1440
# pnpm 10+ reads build-script approvals from here too (array syntax).
